{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Run VehicleMeterSimulator",
            "type": "coreclr",
            "request": "launch",
            "program": "${workspaceFolder}/bin/Debug/net10.0-windows/VehicleMeterSimulator.exe",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        }
    ]
}
