<UserControl x:Class="VehicleMeterSimulator.Views.Controls.IndicatorPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="100"
             d:DesignWidth="560"
             Width="560"
             Height="100"
             Background="Transparent">
    <UserControl.Resources>
        <Style x:Key="IndicatorTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>

    <Border Background="#66050608"
            BorderBrush="#1F252A"
            BorderThickness="1"
            Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border x:Name="ShiftUpIndicatorBorder"
                    Grid.Row="0"
                    Width="150"
                    Height="30"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,10"
                    Background="#0B0B0D"
                    BorderBrush="#2B2B2D"
                    BorderThickness="1">
                <TextBlock x:Name="ShiftUpIndicatorText"
                           Text="SHIFT UP"
                           Foreground="#343438"
                           FontSize="16"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" />
            </Border>

            <UniformGrid Grid.Row="1"
                         Columns="5"
                         HorizontalAlignment="Center">
                <Border x:Name="CheckIndicatorBorder"
                        Width="84"
                        Height="34"
                        Margin="4"
                        Background="#09090B"
                        BorderBrush="#2B2B2D"
                        BorderThickness="1">
                    <TextBlock x:Name="CheckIndicatorText"
                               Text="CHECK"
                               Foreground="#343438"
                               Style="{StaticResource IndicatorTextStyle}" />
                </Border>

                <Border x:Name="AbsIndicatorBorder"
                        Width="84"
                        Height="34"
                        Margin="4"
                        Background="#09090B"
                        BorderBrush="#2B2B2D"
                        BorderThickness="1">
                    <TextBlock x:Name="AbsIndicatorText"
                               Text="ABS"
                               Foreground="#343438"
                               Style="{StaticResource IndicatorTextStyle}" />
                </Border>

                <Border x:Name="OilIndicatorBorder"
                        Width="84"
                        Height="34"
                        Margin="4"
                        Background="#09090B"
                        BorderBrush="#2B2B2D"
                        BorderThickness="1">
                    <TextBlock x:Name="OilIndicatorText"
                               Text="OIL"
                               Foreground="#343438"
                               Style="{StaticResource IndicatorTextStyle}" />
                </Border>

                <Border x:Name="BatteryIndicatorBorder"
                        Width="84"
                        Height="34"
                        Margin="4"
                        Background="#09090B"
                        BorderBrush="#2B2B2D"
                        BorderThickness="1">
                    <TextBlock x:Name="BatteryIndicatorText"
                               Text="BATTERY"
                               Foreground="#343438"
                               Style="{StaticResource IndicatorTextStyle}" />
                </Border>

                <Border x:Name="BrakeIndicatorBorder"
                        Width="84"
                        Height="34"
                        Margin="4"
                        Background="#09090B"
                        BorderBrush="#2B2B2D"
                        BorderThickness="1">
                    <TextBlock x:Name="BrakeIndicatorText"
                               Text="BRAKE"
                               Foreground="#343438"
                               Style="{StaticResource IndicatorTextStyle}" />
                </Border>
            </UniformGrid>
        </Grid>
    </Border>
</UserControl>
